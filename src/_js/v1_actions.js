require('./v1_vendor/typed.min.js');

/*
$(function () {
  var $authorLink = $('.author-link')
  var $downloadLink = $('.buttons #btn-download')
  function gaProxy () {
    if (window.ga) {
      ga.apply(ga, arguments);
    }
  }
  $authorLink.click(function () {
    gaProxy('send', 'event', 'author', 'click')
  })
  $downloadLink.click(function () {
    gaProxy('send', 'event', 'download', 'click')
  })

  var $headerContact = $('header.header-contact')
  function showHideHeaderContact() {
    var scrollTop = $(window).scrollTop()
    if (scrollTop > 300) {
      $headerContact.addClass('is-visible')
    } else {
      $headerContact.removeClass('is-visible')
    }
  }
  $(window).scroll(showHideHeaderContact)
  showHideHeaderContact()

})

*/

$(function () {
})
